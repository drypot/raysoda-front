export function newConfigForClient(config) {
    return {
        appName: config.appName,
        appNamel: config.appNamel,
        appDesc: config.appDesc,
        mainUrl: config.mainUrl,
        uploadUrl: config.uploadUrl
    };
}
//# sourceMappingURL=config.js.map